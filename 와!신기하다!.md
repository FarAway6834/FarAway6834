## 2026.03.02. 11:27

f(a, b) = (Σᵢ₌₁ⁿ aᵢ²)(Σᵢ₌₁ⁿ bᵢ²) = ||a||²||b||²
g(a, b) = (Σᵢ₌₁ⁿ aᵢbᵢ)² = |a • b|²
즉,
|(||a|| ||b||)| ≥ |a • b|
에초에 a • b = ||a|| ||b|| cos θ임.

Aᵢ = aᵢ²
Bᵢ = bᵢ²
cᵢ = aᵢbᵢ
f(a, b) = Σᵢ₌₁ⁿΣⱼ₌₁ⁿAᵢBⱼ = Σᵢ₌₁ⁿAᵢBⁱ + Σᵢ₌₁ⁿΣⱼ₌₁ⁿ δᵢʲAᵢBⱼ
g(a, b) = Σᵢ₌₁ⁿ cᵢ² + Σᵢ₌₁ⁿΣⱼ₌₁ⁿδᵢʲcᵢcⱼ = Σᵢ₌₁ⁿ AᵢBᵢ + Σᵢ₌₁ⁿΣⱼ₌₁ⁿδᵢʲcᵢcⱼ
이다.

aᵢ²bⱼ² + aⱼ²bᵢ² = 2aᵢbⱼaⱼbᵢ + x
aᵢ²bⱼ² + aⱼ²bᵢ² - 2aᵢbⱼaⱼbᵢ = x
 = (aᵢbⱼ - aⱼbᵢ)² ≥ 0
이므로,
aᵢ²bⱼ² + aⱼ²bᵢ² = 2aᵢbⱼaⱼbᵢ + x ≥ 2aᵢbⱼaⱼbᵢ다.

고로,
Σᵢ₌₁ⁿΣⱼ₌₁ⁿ δᵢʲ aᵢ²bⱼ² ≥ Σᵢ₌₁ⁿΣⱼ₌₁ⁿ δᵢʲ aᵢbⱼaⱼbᵢ
이다.
따라서,

f(a, b) ≥ g(a, b)
Q.E.D.

가환환 모델 M에서도, x < x + 1을 만족하는 정렬순서 `<`가 존재하면 코시 슈바르츠 부등식이 성립하는구나... 신기하다.

앗, 곱셈이 덧셈의 반복연산이라는 점을 가정했다는걸 까먹었네, 에초에, x² ≥ 0일것이 보장되어야 하겠구나

## 2026.03.02. 12:00

Notation)
(a ~ b) ≜ (a, b) ∩ ℤ
[a ~ b) ≜ [a, b) ∩ ℤ
(a ~ b] ≜ (a, b] ∩ ℤ
[a ~ b] ≜ [a, b] ∩ ℤ
따라서, (-∞ ~ ∞) = ℤ

본론)
LinearlyIndependent(F, S) : ∀n ∈ ℕ, ∀i ∈ [1 ~ n], ∀aᵢ ∈ F, ∀vᵢ ∈ S, Σᵢ₌₁ⁿ aᵢvᵢ = 0 → aᵢ = 0
LinearlyDependent(F, S) : ¬LinearlyIndependent(F, S)

달리 말하면 ∀n ∈ ℕ, ∀i ∈ [1 ~ n], ∀aᵢ ∈ F, ∀vᵢ ∈ S, (∃aᵢ, aᵢ ≠ 0) → Σᵢ₌₁ⁿ aᵢvᵢ ≠ 0인데, 

해당 경우에 aᵢ = 0인 i는 어짜피 aᵢvᵢ = 0이므로, 항등원으로써 걸러주면,

∀n ∈ ℕ, ∀i ∈ [1 ~ n], ∀aᵢ ∈ F, ∀vᵢ ∈ S, aᵢ ≠ 0 → Σᵢ₌₁ⁿ aᵢvᵢ ≠ 0

와 동치다.

∀n ∈ ℕ, ∀i ∈ [1 ~ n], ∀aᵢ ∈ F, ∀vᵢ ∈ S, aᵢ ≠ 0 → Σᵢ₌₁ⁿ aᵢvᵢ = 0인 경우에 대해 알아보자

∃i, j, aᵢ ≠ 0 ≠ aⱼ이고 aᵢvᵢ + aⱼvⱼ ≠ 0 이라면,
aᵢvᵢ + aⱼvⱼ = bₖvₖ로, 치환했을때 역시 ∀n ∈ ℕ, ∀i ∈ [1 ~ n], ∀aᵢ ∈ F, ∀vᵢ ∈ S, aᵢ ≠ 0 → Σᵢ₌₁ⁿ aᵢvᵢ = 0이여야 하기에,

언젠가는 0 × n꼴로, 식을 n개의 묶음으로 나눌수 있을것이다.

생각하기 귀찮아.

그러므로,

1. ∃i, j, aᵢ ≠ 0 ≠ aⱼ일시 aᵢvᵢ + aⱼvⱼ = 0이거나
2. 불만족시 aᵢvᵢ + aⱼvⱼ = bₖvₖ로, 치환했을때, 1->2 과정에 따라 언젠가는 만족한다.

그러므로, LinearlyIndependent의 경우, 

∀P ⊆ S, ∀u ∈ Span(S \ P), ∀w ∈ Span(P), u + w ≠ 0이라 할수 있겠다.

B isBasisOf (F, V) : Span(B) = V, LinearlyIndependent(F, B)
이다.

여기서 알수있는 사실은 LinearlyIndependent(F, V) ∧ ¬ B isBasisOf (F, V) : Span(B) ≠ V, LinearlyIndependent(F, B)
라는거다.

뭐.... 그래봤자 B ⊆ V면, Span(B) ≤ V이기에, Span(B) ≤ V, Span(B) ≠ V면, B ⊆ V인 경우, ∃W ≤ V, B isBasisOf (F, W)다.

즉, S가 선형 독립임은, S가 W의 기저인 벡터공간 W가 존재함을 의미한다.

## 2026.03.02 12:07

직교 기저 (orthogonal basis)

직교인 기저다. 크헤헤헤헤헤

험.험. 

v랑 w가 직교인 경우는 뭘까?

v ⟂ w : v • w = 0

그렇다. 도트곱이 0인거다. cos 90° = 0이잖는가.

엄... 일등급이면 모를리가 없지.

...

암튼 `⟂`는 모순이라고 엿먹으라는 기호랑은 다른 U+27C2에 배정된 직교 전용 기호라는점 명시하겠다.